export enum PublicAppRoutes {
  HOME = "/",
  LOGIN = "/login",
  PAGE_NOT_FOUND = "/*",
  JOIN_SESSION = "/join",
  JOIN_SESSION_DIRECTLY = "/join/:joinId",
  QUIZ_SESSION_PAGE = "/session/:joinId",
}
